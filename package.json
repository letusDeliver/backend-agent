{
  "name": "backend-engineering-agent-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Backend Engineering Agent Platform MVP — Angular UI, Express orchestrator, Claude Code execution layer.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --workspace server\" \"npm run start --workspace web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run start --workspace web",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "test:server": "npm run test --workspace server",
    "test:web": "npm run test --workspace web",
    "lint": "npm run lint --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "rxjs": "7.8.1"
  }
}
