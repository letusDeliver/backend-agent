# Task Workspace

Each task gets an isolated workspace:

tasks/<task-id>/
├── task.json
├── context/
├── specialist-reports/
├── reconciliation.json
├── implementation-plan.json
├── execution-report.json
├── reviews/
└── final-handoff.md

Task artifacts are disposable operational records unless explicitly promoted into validated memory.
